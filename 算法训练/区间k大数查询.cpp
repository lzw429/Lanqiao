#include <iostream>

using namespace std;

int main() {
    int n, m;
    int l, r, K;
    int i;
    cin >> n;
    int a[n + 5];
    for (i = 0; i != n; ++i)
        cin >> a[i];
    cin >> m;
    for (i = 0; i != m; ++i) {
        cin >> l >> r >> K;
        for (int j = l; j <= r; ++j) {

        }
    }
    return 0;
}